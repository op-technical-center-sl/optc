<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <data>

        <!--
            Link CSS to assets
        -->

        <template id="report_assets_mcr" inherit_id="web.report_assets_common" priority="1">
            <xpath expr="//link[last()]" position="after">
                <link rel="stylesheet" type="text/css" href="/mcr_reports/static/src/css/web_external_layout.scss"/>
            </xpath>
        </template>

        <!--
            Custom external layout
        -->

        <template id="external_layout_mcr">
            <div id="mcr-layout" t-attf-class="header o_company_#{company.id}_layout border-bottom border-dark" t-att-style="report_header_style">
                <div class="row">
                    <div id="company-logo" class="col-8">
                        <img t-if="company.logo" t-att-src="image_data_uri(company.logo)" alt="Company Logo"/>
                    </div>

                    <div id="iatf-logo" class="col-1">
                        <img t-if="company.report_secondary_logo" t-att-src="image_data_uri(company.report_secondary_logo)" alt="Secondary Logo"/>
                    </div>

                    <div class="col-3 text-right">
                        <span t-field="company.name" />
                        <br />
                        <span t-field="company.street" />
                        <br />
                        <span t-field="company.zip" />
                        <span t-field="company.city" />
                        <br />
                        <span t-field="company.country_id.name" />
                        <t t-if="company.rex_number and o.partner_id.show_rex"><br />Nro REX: <span t-field="company.rex_number" /></t>
                        <t t-else=""><br /></t>
                        <t t-if="company.vat"><br />VAT: <span t-field="company.vat"/></t>
                    </div>
                </div>
            </div>

            <!--
                From this point onwards, layout is copy-pasted from web.external_layout_standard.
                Edit if necessary.
            -->

            <div t-attf-class="article o_report_layout_standard o_company_#{company.id}_layout"  t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id" t-att-data-oe-lang="o and o.env.context.get('lang')">
                <t t-call="web.address_layout"/>
                <t t-raw="0"/>
            </div>

            <div t-attf-class="footer o_standard_footer o_company_#{company.id}_layout">
                <div class="text-center" style="border-top: 1px solid black;">
                    <ul class="list-inline mb4">
                        <!-- using the list-inline-item class from bootstrap causes weird behaviours in pdf report
                              adding d-inline class fixes the problem-->
                        <li t-if="company.phone" class="list-inline-item d-inline">Phone: <span class="o_force_ltr" t-field="company.phone"/></li>
                        <li t-if="company.email" class="list-inline-item d-inline">Email: <span t-field="company.email"/></li>
                        <li t-if="company.website" class="list-inline-item d-inline">Web: <span t-field="company.website"/></li>
                        <li t-if="company.vat" class="list-inline-item d-inline"><t t-esc="company.country_id.vat_label or 'Tax ID'"/>: <span t-field="company.vat"/></li>
                    </ul>

                    <div name="financial_infos">
                        <span t-field="company.report_footer"/>
                    </div>

                    <div t-if="report_type == 'pdf'" class="text-muted">
                        Page: <span class="page"/> / <span class="topage"/>
                    </div>
                </div>
            </div>
        </template>
    </data>
</odoo>
